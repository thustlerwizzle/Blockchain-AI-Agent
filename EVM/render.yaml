services:
  - type: web
    name: blockchain-ai-dashboard
    env: bun
    buildCommand: bun install
    startCommand: bun run start:dashboard
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
    healthCheckPath: /api/test

